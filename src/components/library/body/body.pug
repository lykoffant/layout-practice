//-
  options.pixelPerfectDesign  - enable/disable fixed container width for pixel 
                                perfect design (boolean)

mixin body(options = {})
  -
    let classes = [];

    if (options.pixelPerfectDesign === true) {
      classes.push('body_pixel-perfect-design');
    }

  body.body(class=classes)
    block
